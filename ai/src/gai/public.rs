/*
 Similar to struct InnerSpace of persona.rs, we can custom build these structs of custom LLM.
 In each struct, we use enum for required types and mods for graph directions.
 Reasoning feedback from normative Intelligence is at the core of every specialized GAI 
 https://www.youtube.com/watch?v=cyGZPF_RMNE, and reversed engineering from causal Dependent 
 Nature to be statistically tested at the reasoning of custom LLM in specialized fields as a
 Service.
 
 Via Mu front-end container, a Serverless App uses cloudevents and WebSockets to access this
 platform GAI Kp -> ai as the gateway to legally binding of connected services of the persona
 and its binding Activity, Relationship and Place. Each persona has 2 custom LLMs in its 
 registered donain name conncted to unique KpId: Inner and Outer worlds. By default, contracted
 activities of the persona are replicated into the involved party Kp Inner and Outer worlds
*/
#[derive(Debug)]
pub struct Kp {}

// directed to following mods
#[derive(Debug)]
pub struct KpInner {}

#[derive(Debug)]
pub struct KpOuter {}

// other specialized nodes are:
#[derive(Debug)]
pub struct KpWellness {}

#[derive(Debug)]
pub struct KpPlatform {}

#[derive(Debug)]
pub struct KpMu {}

#[derive(Debug)]
pub struct KpPMO {}

#[derive(Debug)]
pub struct KpSIS {}

#[derive(Debug)]
pub struct DeepSeek {}

#[derive(Debug)]
pub struct Granite {}

#[derive(Debug)]
pub struct Llama {}

#[derive(Debug)]
pub struct Phi {}

#[derive(Debug)]
pub struct Grok {}


